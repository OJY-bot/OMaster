# 此文件已废弃，请使用 apk-release.yml
# 保留此文件以防止GitHub Actions错误

name: Deprecated

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "This workflow is deprecated. Please use apk-release.yml"
